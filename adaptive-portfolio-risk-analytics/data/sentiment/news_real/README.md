# Real News Intake

Create `manifest.csv` from `manifest_template.csv`. Keep licensed/private raw
exports under `raw/`; that directory is ignored by Git. The example manifest is
an explicit placeholder and is excluded from real-data validation.
