# Stage Reports

These reports preserve the implementation history for Stages 1 through 14. They describe the
repository at the time each stage was completed and are not the canonical statement of current
capability. Use the root `README.md`, `project_explainer.html`, and `docs/PROJECT_AUDIT.md` for the
current project boundary.

